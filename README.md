Jacto: JUnit AspectJ Coverage Tool
==================================

Uses AspectJ to observe method calls during JUnit test execution and reports
via a local socket.


Development
-----------

Generate Eclipse IDE files:

    mvn eclipse:eclipse -DdownloadJavadocs=true

Compile a jar:

    mvn clean package
